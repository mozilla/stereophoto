<h2 id="camera">Camera stream from comp</h2>
{{#if left}}
	<button {{ action 'takeLeftPhoto' }}>take left photo</button>
{{else}}
	<button {{ action 'takeRightPhoto' }}>take right photo</button>
{{/if}}

